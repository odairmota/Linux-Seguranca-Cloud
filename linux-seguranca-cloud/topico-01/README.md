# Tópico 1 - Preparação do ambiente Linux

## Ambiente utilizado
Foi uma VM local Ubuntu Server 2025.
## Objetivo desta atividade
Preparar o ambiente Linux e organizar a primeira evidência técnica.
## Estrutura criada
Foi criado uma pasta principal da disciplina(linux-seguranca-cloud),
 nele foi criado duas pastas uma para guardar os conteúdos com o nome
 tópico-01 e outra para armazenar o produto final. Dentro da pasta tópico-01
 foi criado uma pasta evidencias, um ficheiro com o nome comandos-topicos-01.txt
 para armazenar os comandos básicos utilizados e também foi criado um ficheiro README.md
 para guardar a descrição do projeto dessa disciplina.

## Comandos executados
1. pwd      - Mostra o caminho completo do diretório atual onde se encontra.
2. ls       - Lista o conteúdo (ficheiros e pastas) do diretório atual.
3. cd       - Altera o diretório atual (ex: "cd /var/log" para entrar nessa pasta).
4. mkdir    - Cria uma nova pasta/diretório (ex: "mkdir NovaPasta").
5. touch    - Cria um ficheiro vazio ou atualiza a data de modificação de um existente.
6. rm       - Apaga ficheiros ou pastas (ex: "rm ficheiro.txt" ou "rm -r pasta").
7. cp       - Copia ficheiros ou pastas de um local para outro (ex: "cp orig.txt copia.txt").
8. mv       - Move ou renomeia ficheiros ou pastas (ex: "mv antigo.txt novo.txt").
## Diferença entre VM, VPS e infraestrutura em nuvem
Explicar de forma curta, com foco no contexto deste módulo.

## Dificuldades encontradas
Algumas dificuldades com os comandos.

## Próximos passos
Ter a maquina preparada com os recursos para criação de ficheiro e ferramentas para ter conexão remota tais como SSH.
